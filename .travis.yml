language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mroziu/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true mroziu/react-test npm test

after_success:
  - docker build -t mroziu86/multi-client ./client
  - docker build -t mroziu86/multi-nginx ./nginx
  - docker build -t mroziu86/multi-server ./server
  - docker build -t mroziu86/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push mroziu86/multi-client
  - docker push mroziu86/multi-nginx
  - docker push mroziu86/multi-server
  - docker push mroziu86/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'eu-north-1'
  app: 'multi-docker'
  env: 'Multi-docker-env'
  bucket_name: 'elasticbeanstalk-eu-north-1-167632905528'
  bucket_path: 'docker-multi'
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY